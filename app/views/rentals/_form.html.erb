 <%= simple_form_for [tool, rental] do |f| %>
  <%= f.input :start_date, as: :string, input_html: { data: { controller: "datepicker" }, placeholder: "Select a date range" } %>
  <%= f.button :submit, "Reserve", class: "btn btn-info"%>
<% end %>
