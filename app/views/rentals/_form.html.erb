 <%= simple_form_for [tool, rental] do |f| %>
  <%= f.input :start_date, label: "Dates", as: :string, input_html: { data: { controller: "datepicker" }, placeholder: "Select Your Dates" } %>
  <%= f.button :submit, "Reserve", class: "btn btn-info"%>
<% end %>
