 <%= simple_form_for @tool do |f| %>
  <%= f.input :opening_date,
              as: :string,
              input_html: { data: { controller: "datepicker" },
              placeholder: "Select a date range" } %>
  <%=  f.button :submit, "Reserve", class: "btn btn-gradient"%>
<% end %>
